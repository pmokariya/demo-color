<%= form_with(model: secondary_color, local: true) do |form| %>
  <% if secondary_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(secondary_color.errors.count, "error") %> prohibited this secondary_color from being saved:</h2>

      <ul>
      <% secondary_color.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :hash_code %>
    <%= form.text_field :hash_code %>
  </div>

  <div class="field">
    <%= form.label :primary_color_id %>
    <%= form.text_field :primary_color_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
